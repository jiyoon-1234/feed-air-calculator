services:
  - type: web
    name: feed-air-calculator
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python outputs/feed_air_server.py --no-browser
    plan: free
